import React from 'react'

const Loader = () => {
    return (
        <div className='options-autoload-manager_loader'>
            <img src={ OPTIONS_AUTOLOAD_MANAGER.asseturl + '/img/loader.gif'} />
        </div>
     );
}

export default Loader;